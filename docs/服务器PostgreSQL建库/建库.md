# 语法：ssh -L 本地端口:localhost:目标端口 用户名@服务器IP
ssh -L 54321:localhost:5432 user@10.10.0.5
执行后： 只要这个 SSH 窗口不关，成员的电脑上就会多出一个虚拟的 127.0.0.1:54321。

配置项目： 他们只需要把本地的 .env 改成：
DB_HOST=127.0.0.1
DB_PORT=54321

原理： 他们访问本地的 54321，数据会通过加密的 SSH 通道安全地传给服务器，然后由服务器“代表”他们去访问数据库的 5432 端口。